---
import { getCollection, render } from "astro:content";
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import ArrowCard from "@components/ArrowCard.astro";
import Link from "@components/Link.astro";
import { dateRange } from "@lib/utils";
import { SITE, HOME, SOCIALS } from "@consts";
import SlicewiseLogo from "../../public/featured/slicewise-logo.svg";
import DioramaLogo from "../../public/featured/diorama-logo.svg";
import ObtegoLogo from "../../public/featured/obtego-logo.svg";

// const blog = (await getCollection("blog"))
//   .filter(post => !post.data.draft)
//   .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
//   .slice(0,SITE.NUM_POSTS_ON_HOMEPAGE);

const projects = (await getCollection("projects"))
  .filter(project => !project.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_PROJECTS_ON_HOMEPAGE);

const talks = (await getCollection("talks"))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0,SITE.NUM_TALKS_ON_HOMEPAGE);

const allwork = (await getCollection("work"))
  .sort((a, b) => new Date(b.data.dateStart).valueOf() - new Date(a.data.dateStart).valueOf())
  .slice(0,SITE.NUM_WORKS_ON_HOMEPAGE);

const work = await Promise.all(
  allwork.map(async (item) => {
    const { Content } = await render(item);
    return { ...item, Content };
  })
);

const currentJob = work.find((item) => item.data.dateEnd === "Present");
if (currentJob) {
  const [item] = work.splice(work.indexOf(currentJob), 1);
  work.unshift(item);
}
---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <h4 class="animate font-semibold font-monospace text-2xl">
      Hi, I'm Josh Quintal
    </h4>
    <div class="space-y-16">
      <section>
        <article class="animate space-y-4">
          <p>
            I'm a technically grounded product leader who began as a software engineer, scaled developer infrastructure products in web3, and now builds AI-native healthcare systems. Experienced in translating complex technical capabilities into production products, from developer platforms serving millions of requests to agentic workflows that reason over longitudinal medical records and patient-facing clinical content.
          </p>
          <p>
            我会说中文。
          </p>
        </article>
      </section>

      
      <section class="active-projects">
        <h5 class="animate font-semibold font-monospace text-xl mb-6">
          Active Work & Projects
        </h5>
        <nav class="grid grid-cols-3 gap-4">
          <a href="https://github.com/DiscRisknBisque/slicewise" target="_blank" rel="noopener noreferrer">
            <article class="animate h-full border dark:border-dblue-400 p-4 rounded-lg flex items-center justify-center hover:bg-white/5">
              <span role="img" aria-label="Slicewise"><SlicewiseLogo class="w-34" /></span>
            </article>
          </a>
          <a href="https://diorama.build" target="_blank" rel="noopener noreferrer">
            <article class="animate h-full border dark:border-dblue-400 p-4 rounded-lg flex items-center justify-center hover:bg-white/5">
              <span role="img" aria-label="Diorama"><DioramaLogo class="w-full" /></span>
            </article>
          </a>
          <a href="https://obtego.io" target="_blank" rel="noopener noreferrer">
            <article class="animate h-full border dark:border-dblue-400 p-4 rounded-lg flex items-center justify-center hover:bg-white/5">
              <span role="img" aria-label="Obtego"><ObtegoLogo class="w-30" /></span>
            </article>
          </a>
        </nav>
      </section>
      
      <!-- <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold">
            Latest posts
          </h5>
          <Link href="/blog">
            See all posts
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold">
            Recent talks
          </h5>
          <Link href="/talks">
            See all talks
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {talks.map(talk => (
            <li>
              <ArrowCard entry={talk} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold">
            Recent projects
          </h5>
          <Link href="/projects">
            See all projects
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {projects.map(project => (
            <li>
              <ArrowCard entry={project} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold">
            Work Experience
          </h5>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="flex flex-col space-y-4">
          {work.map(entry => (
            <li>
              <div class="text-sm opacity-75">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <div class="font-semibold">
                {entry.data.company}
              </div>
              <div class="text-sm opacity-75">
                {entry.data.role.join(", ")}
              </div>
              <article>
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section> -->

      <section class="animate space-y-4">
        <h5 class="font-semibold font-monospace text-xl">
          Let's Connect
        </h5>
        <article>
          <p>
            If you want to get in touch with me about something or just to say hi,
            reach out on social media or send me an email.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              {SITE.EMAIL}
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
